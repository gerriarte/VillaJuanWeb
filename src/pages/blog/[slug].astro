---
import Base from '../../layouts/Base.astro';

export function getStaticPaths() {
  // TODO(post-scaffold): generar rutas desde Directus / content collections.
  // Sin posts todavía → no se genera ninguna página (stub válido).
  return [];
}

const { slug } = Astro.params;
---

<Base title="Artículo · Blog Villa Juan">
  <article class="mx-auto max-w-3xl px-4 py-16">
    <h1 class="font-display text-4xl text-cta">Artículo</h1>
    <p class="mt-4 text-ink/60">Plantilla de post (stub) — slug: {slug}</p>
  </article>
</Base>
