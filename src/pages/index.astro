---
import Base from '../layouts/Base.astro';
import { Image } from 'astro:assets';
import HeroCarousel from '../components/sections/HeroCarousel.astro';
import Faq from '../components/sections/Faq.astro';
import { visit, wa, social } from '../lib/site';
import pdfMenu from '../assets/images/villa-planes/Menu_Villa_Juan_Sin_Precios.pdf?url';
import fuenteCaballos from '../assets/images/home/fuente_caballos.jpg';
import horseCronicas from '../assets/images/home/horse_cronicas.png';
import tituloCronicas from '../assets/images/home/Titulo_cronicas.png';
import postCoaching from '../assets/images/home/hero_home_empresariales.jpg';
import postBarril from '../assets/images/home/hero_home_restaurante.jpg';
import horseImg from '../assets/images/home/bienvenida_horse.svg';
import globoImg from '../assets/images/home/bienvenida_globo.svg';
import expBanner from '../assets/images/home/ecogranja_experiencia_transformadora.svg';
import ramaImg from '../assets/images/home/rama_oferta_tight.webp';
import celebracionesOferta from '../assets/images/home/celebraciones_oferta.svg';
import empresasOferta from '../assets/images/home/empresas_oferta.svg';
import restauranteOferta from '../assets/images/home/restaurante_oferta.svg';
import blogOferta from '../assets/images/home/blog_ofertas.svg';
import barrilOff from '../assets/images/home/barril_off.svg';
import barrilOn from '../assets/images/home/barrill_on.svg';
import menuJeep from '../assets/images/home/menu_jeep.svg';
import sombrero from '../assets/images/home/sobrero.svg';
import tituloVillaDatos from '../assets/images/home/Titulo-Villa-Datos.svg';
import horseDatos from '../assets/images/home/horse_datos.svg';
import selfieAnimales from '../assets/images/home/selfie_con_animales.png';
import contactanos from '../assets/images/home/contactanos.png';
import villaPlanesTitulo from '../assets/images/home/villa-planes-titulo.png';
import galeria01 from '../assets/images/home/galeria01.png';
import galeria02 from '../assets/images/home/galeria02.png';
import galeria03 from '../assets/images/home/galeria03.png';

// Composición sobre la rama: Empresas ENCIMA; Pasadías, Celebraciones y Blog DEBAJO.
// `pos` = posición absoluta (desktop). Las clases se escriben literales para que Tailwind las genere.
const ofertaEmpresas = { img: empresasOferta, href: '/empresas', label: 'Empresas', w: 459, h: 535 };
const ofertasAbajo = [
  { img: restauranteOferta, href: '/villa-planes', label: 'Pasadías', w: 569, h: 525, pos: 'left-[18%] top-[48%]' },
  { img: celebracionesOferta, href: '/celebraciones', label: 'Celebraciones', w: 524, h: 581, pos: 'left-[40%] top-[52%]' },
  { img: blogOferta, href: '/blog', label: 'Blog', w: 449, h: 577, pos: 'left-[62%] top-[48%]' },
];
const ofertasMobile = [ofertaEmpresas, ...ofertasAbajo];

const stats = [
  { n: 1000, label: 'Eventos corporativos' },
  { n: 5000, label: 'Árboles sembrados' },
  { n: 100, label: 'Animales por conocer' },
  { n: 20000, label: 'Niños felices' },
  { n: 5000, label: 'Familias sonrientes' },
];
// Formato con separador de miles "." (manual, sin depender del ICU del build).
const fmt = (n: number): string => '+' + n.toString().replace(/\B(?=(\d{3})+(?!\d))/g, '.');

// Crónicas de la Tierra — posts destacados del blog en el home.
// TODO(post-scaffold): traer estos posts desde Directus / content collections; href al slug real.
const cronicas = [
  {
    img: postCoaching,
    alt: 'Equipo corporativo celebrando en Villa Juan',
    title: 'Los Beneficios del Coaching Equino para Ejecutivos',
    excerpt: 'Descubre insights únicos sobre coaching desde Villa Juan.',
    href: '/blog',
  },
  {
    img: postBarril,
    alt: 'Zona de asados al barril en Villa Juan',
    title: 'Secretos de la Cocción al Barril: Técnica Artesanal',
    excerpt:
      'Descubre insights únicos sobre gastronomía desde Villa Juan, experiencias auténticas y consejos prácticos para el crecimiento personal.',
    href: '/blog',
  },
];

// Villa Planes en el home: botones-píldora con degradé por plan.
// TODO(post-scaffold): cuando existan los PDFs (Villa_Plan_*.pdf), apuntar el href a la
// descarga real con `download`. Por ahora la conversión va por WhatsApp (invariante de CTAs).
const planesHome = [
  { title: 'Paso Fino', grad: 'linear-gradient(90deg,#f2c14e 0%,#d99323 100%)' },
  { title: 'Trocha y Galope', grad: 'linear-gradient(90deg,#e0431f 0%,#c0360d 100%)' },
  { title: 'Trote y Galope', grad: 'linear-gradient(90deg,#4ba82b 0%,#00811e 100%)' },
];

// "¿Cómo llegar desde Bogotá?" — 3 opciones de transporte con chip informativo/CTA.
const llegar: {
  title: string;
  body: string;
  pill: string;
  pillHref?: string;
}[] = [
  {
    title: 'Carro',
    body: 'Búscanos en Waze o Google Maps como Ecogranja Villa Juan.',
    pill: '45 minutos aprox.',
    pillHref: visit.maps,
  },
  {
    title: 'Transporte público',
    body: 'Portal 80 o Portal Norte → bus intermunicipal hasta el pueblo de Tenjo → taxi hasta Villa Juan.',
    pill: '1 hora 15 min aprox.',
  },
  {
    title: 'Transporte privado',
    body: 'Organizamos transporte grupal para tu pasadía o evento, sin complicaciones.',
    pill: 'Consultar disponibilidad',
    pillHref: wa('Quiero información del transporte a Villa Juan'),
  },
];

// Galería — carrusel "coverflow": las fotos rotan por 3 posiciones (centro al frente +
// laterales detrás, como el mockup). Estilos base (también sirven de fallback sin JS).
const galeria = [
  { img: galeria02, alt: 'Una visitante saludando a un burro en Villa Juan, con la sabana de fondo' },
  { img: galeria01, alt: 'La escultura del caballo de Villa Juan entre las zonas verdes de la ecogranja' },
  { img: galeria03, alt: 'Un perro corriendo feliz por las zonas verdes de Villa Juan' },
];
const galPos = {
  center: 'transform:translate(-50%,-50%) scale(1);z-index:30;opacity:1;filter:none;',
  right: 'transform:translate(-6%,-50%) scale(0.82) rotateY(-18deg);z-index:20;opacity:0.78;filter:brightness(0.82);',
  left: 'transform:translate(-94%,-50%) scale(0.82) rotateY(18deg);z-index:20;opacity:0.78;filter:brightness(0.82);',
};
// Orden inicial (SSR / sin JS): 0 = centro, 1 = derecha, 2 = izquierda.
const galInit = [galPos.center, galPos.right, galPos.left];
---

<Base heroHeader>
  <!-- HERO — carrusel de 4 slides detrás del header (que flota con degradé verde→transparente). -->
  <HeroCarousel />

  <!-- EXPERIENCIAS TRANSFORMADORAS (bienvenida): banner + título + caballo + globo. -->
  <section class="mx-auto max-w-6xl px-4 py-16">
    <img
      src={expBanner.src}
      alt="Ecogranja · Experiencia transformadora"
      width="719"
      height="84"
      class="mx-auto mb-4 h-auto w-full max-w-md"
    />
    <h2 class="section-title text-center">
      Experiencias transformadoras en conexión con la naturaleza
    </h2>

    <div class="mt-10 grid items-center gap-6 md:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)] md:gap-2">
      <img
        src={horseImg.src}
        alt="Don Juan, la mascota de la Ecogranja Villa Juan"
        width="522"
        height="501"
        loading="lazy"
        class="mx-auto w-48 sm:w-64 md:w-full md:max-w-sm md:translate-x-[20%]"
      />

      <!-- Globo de diálogo (imagen de fondo estirada) + texto blanco centrado dentro. -->
      <div
        class="flex items-center justify-center px-8 py-8 sm:px-12 sm:py-10"
        style={`background-image:url('${globoImg.src}');background-size:100% 100%;background-repeat:no-repeat;`}
      >
        <p class="w-[70%] text-center font-['Segoe_UI',system-ui,sans-serif] text-sm leading-relaxed text-white [text-shadow:0_1px_3px_rgba(0,0,0,0.5)] sm:text-base">
          Te invitamos a conocer la Ecogranja Villa Juan, un lugar increíble para salir de la rutina
          y reconectar con lo natural. Comparte con nuestros animales, aprende sobre el mundo equino y
          disfruta de los quehaceres reales del campo. Y no te puedes perder el sabor único de nuestro
          barril peruano. ¡Vente con tu familia a vivir un día de campo inolvidable!
        </p>
      </div>
    </div>
  </section>

  <!-- EXPLORA LO QUE OFRECEMOS — ofertas sobre una rama que sangra a la izquierda. -->
  <section class="overflow-hidden pb-16">
    <h2 class="mx-auto max-w-6xl px-4 text-center section-title">
      Explora lo que ofrecemos
    </h2>

    <!-- Desktop: Empresas ENCIMA de la rama; Pasadías, Celebraciones y Blog DEBAJO. -->
    <div class="relative mt-4 hidden h-[560px] w-full md:block lg:h-[720px]">
      <img
        src={ramaImg.src}
        alt=""
        aria-hidden="true"
        width="1429"
        height="445"
        class="pointer-events-none absolute top-1/2 left-0 w-[80%] -translate-y-1/2"
      />
      <a
        href={ofertaEmpresas.href}
        aria-label={ofertaEmpresas.label}
        class="absolute top-[1%] left-[36%] z-10 w-64 transition-transform duration-300 hover:-translate-y-2 lg:w-80"
      >
        <img
          src={ofertaEmpresas.img.src}
          alt={ofertaEmpresas.label}
          width={ofertaEmpresas.w}
          height={ofertaEmpresas.h}
          loading="lazy"
          class="w-full drop-shadow-xl"
        />
      </a>
      {
        ofertasAbajo.map((o) => (
          <a
            href={o.href}
            aria-label={o.label}
            class:list={[
              'absolute z-10 w-64 transition-transform duration-300 hover:-translate-y-2 lg:w-80',
              o.pos,
            ]}
          >
            <img src={o.img.src} alt={o.label} width={o.w} height={o.h} loading="lazy" class="w-full drop-shadow-xl" />
          </a>
        ))
      }
    </div>

    <!-- Mobile: grid simple de las 4 ofertas. -->
    <ul class="mx-auto mt-8 grid max-w-md grid-cols-2 gap-6 px-4 md:hidden">
      {
        ofertasMobile.map((o) => (
          <li>
            <a href={o.href} aria-label={o.label} class="block transition-transform hover:-translate-y-1">
              <img src={o.img.src} alt={o.label} width={o.w} height={o.h} loading="lazy" class="mx-auto w-40 drop-shadow-xl" />
            </a>
          </li>
        ))
      }
    </ul>
  </section>

  <!-- BANDA negro → rojo → verde detrás de Sabor de fuego, Celebraciones y Villa Datos.
       Los extremos transparentes revelan el fondo verde continuo del sitio (blend). -->
  <div
    class="relative"
    style="background-image:linear-gradient(180deg, transparent 0%, #0d0605 14%, #0d0605 30%, #5c0f06 48%, #a31d0b 68%, #a31d0b 90%, transparent 100%);"
  >
    <!-- SABOR DE FUEGO (barril) — centrado; el barril se "enciende" con el scroll. -->
    <section class="mx-auto max-w-3xl px-4 pt-16 pb-16 text-center text-white">
      <div class="relative mx-auto mb-6 w-52 sm:w-64">
        <img
          src={barrilOff.src}
          alt="Barril de asado al carbón"
          width="507"
          height="546"
          loading="lazy"
          class="w-full"
        />
        <img
          src={barrilOn.src}
          alt=""
          aria-hidden="true"
          width="507"
          height="546"
          loading="lazy"
          class="barril-on absolute inset-0 w-full"
        />
      </div>
      <h2 class="section-title">Sabor de fuego</h2>
      <p class="mx-auto mt-4 max-w-2xl font-bold leading-relaxed text-white/90">
        La técnica es nuestra firma. Seleccionamos cortes de alta calidad con maduración controlada
        para intensificar cada matiz. Cocinamos al barril mediante calor indirecto con carbón vegetal;
        un proceso de horas que garantiza una jugosidad imposible de lograr en una parrilla
        convencional.
      </p>
      <a
        href={pdfMenu}
        target="_blank"
        rel="noopener noreferrer"
        aria-label="Ver el menú"
        class="mt-6 inline-block transition-transform hover:-translate-y-1 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-white"
      >
        <img
          src={menuJeep.src}
          alt="Ver el menú"
          width="694"
          height="397"
          loading="lazy"
          class="mx-auto w-56 drop-shadow-xl sm:w-64"
        />
      </a>
    </section>

    <!-- CELEBRACIONES CON PROPÓSITO -->
    <section class="mx-auto max-w-4xl px-4 py-16 text-center">
      <h2 class="section-title">Celebraciones con propósito</h2>
      <p class="mt-4 text-lg font-bold leading-relaxed text-white/90">
        En la Ecogranja Villa Juan no solo alquilamos un espacio; diseñamos escenarios naturales para
        fortalecer equipos y celebrar la vida. Contamos con amplias zonas verdes y espacios cubiertos
        ideales para momentos únicos.
      </p>
      <img
        src={sombrero.src}
        alt=""
        aria-hidden="true"
        width="1040"
        height="585"
        loading="lazy"
        class="mx-auto mt-8 w-[32rem] max-w-full drop-shadow-xl sm:w-[40rem]"
      />
      <a
        href="/celebraciones"
        class="mt-4 inline-flex items-center justify-center rounded-full bg-brand-strong px-12 py-6 font-display text-3xl text-white ring-1 ring-white/30 transition hover:brightness-110 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white sm:text-4xl"
      >
        Conocer eventos
      </a>
    </section>

    <!-- VILLA DATOS -->
    <section class="mx-auto max-w-6xl px-4 pt-16 pb-28 text-center text-white">
      <h2>
        <img
          src={tituloVillaDatos.src}
          alt="Villa Datos curiosos"
          width="539"
          height="462"
          loading="lazy"
          class="mx-auto w-56 drop-shadow-lg sm:w-64"
        />
      </h2>
      <img
        src={horseDatos.src}
        alt=""
        aria-hidden="true"
        width="1190"
        height="578"
        loading="lazy"
        class="mx-auto mt-2 w-full max-w-3xl"
      />
      <dl id="villa-datos" class="mt-10 grid grid-cols-2 gap-6 sm:grid-cols-3 lg:grid-cols-5">
        {
          stats.map((s, i) => (
            <div
              class="stat-circle mx-auto flex aspect-square w-full max-w-[13rem] flex-col items-center justify-center rounded-full bg-[#E3542A] p-6 text-center text-white shadow-xl ring-4 ring-white/15"
              style={`transition-delay:${i * 90}ms`}
            >
              <dt class="font-display text-sm leading-[0.95] [text-shadow:0_1px_2px_rgba(0,0,0,0.25)] sm:text-base">
                {s.label}
              </dt>
              <dd
                class="mt-2 text-2xl font-bold [text-shadow:0_1px_2px_rgba(0,0,0,0.25)] lg:text-3xl"
                data-count={s.n}
              >
                {fmt(s.n)}
              </dd>
            </div>
          ))
        }
      </dl>
    </section>
  </div>

  <!-- MUSEO DEL CABALLO — próximamente (panel naranja sobre el verde del sitio). -->
  <section class="mx-auto max-w-5xl px-4 py-8">
    <div class="rounded-[2.5rem] bg-cta-strong px-6 py-10 shadow-xl sm:px-10 md:px-12 md:py-14">
      <h2 class="text-center font-display text-3xl leading-tight text-[#FFF200] [text-shadow:0_2px_4px_rgba(0,0,0,0.35)] sm:text-4xl md:text-5xl">
        ¡Una nueva aventura está por galopar!
      </h2>

      <div class="mt-8 grid items-center gap-8 md:grid-cols-2 md:gap-10">
        <!-- Texto -->
        <div class="text-center text-white [text-shadow:0_1px_2px_rgba(0,0,0,0.35)] md:text-left">
          <p class="text-base leading-relaxed sm:text-lg">
            ¡Descubre el nuevo <strong>Museo del Caballo</strong> en Ecogranja Villa Juan!
            Explora el vínculo histórico entre humanos y equinos en un espacio diseñado para
            todas las edades. Aprende sobre su biología, comportamiento e impacto ambiental en un
            entorno natural único. Si buscas la mejor salida pedagógica o una experiencia de
            aprendizaje interactivo, ¡este será tu lugar favorito!
          </p>
          <p class="mt-6 font-bold sm:text-lg">
            Muy pronto, la magia de los caballos a tu alcance.
          </p>
        </div>

        <!-- Foto enmarcada + botón -->
        <div class="relative mx-auto w-full max-w-sm pb-6">
          <div class="overflow-hidden rounded-[2rem] bg-white p-2 shadow-lg ring-1 ring-black/10">
            <Image
              src={fuenteCaballos}
              alt="Monumento a los caballos en la Ecogranja Villa Juan"
              widths={[400, 640, 900]}
              sizes="(min-width: 768px) 40vw, 90vw"
              format="webp"
              quality={72}
              loading="lazy"
              class="h-64 w-full rounded-[1.5rem] object-cover sm:h-72"
            />
          </div>
          <a
            href="#notificarme"
            class="absolute bottom-0 left-1/2 inline-flex -translate-x-1/2 items-center justify-center rounded-full bg-brand-strong px-8 py-3 font-display text-xl text-white shadow-lg ring-1 ring-white/30 transition hover:brightness-110 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
          >
            Notificarme Apertura
          </a>
          <!-- TODO: "Notificarme" abre un popup que capta Nombre, WhatsApp y Correo al mail de Villa Juan. -->
        </div>
      </div>
    </div>
  </section>

  <!-- CRÓNICAS DE LA TIERRA — blog destacado (panel naranja + caballo recostado arriba). -->
  <section class="mx-auto max-w-5xl px-4 pb-8 pt-36 sm:pt-44 md:pt-52">
    <div class="relative rounded-[2.5rem] bg-cta-strong px-5 pb-10 pt-28 shadow-xl sm:px-10 sm:pt-32 md:px-12">
      <!-- Caballo recostado: sobresale por el borde superior del panel. -->
      <Image
        src={horseCronicas}
        alt="Juan, la mascota de Villa Juan, recostado"
        widths={[400, 640, 900]}
        sizes="(min-width: 768px) 36rem, 20rem"
        format="webp"
        quality={80}
        loading="lazy"
        class="pointer-events-none absolute -top-32 left-1/2 w-80 -translate-x-1/2 [filter:drop-shadow(0_8px_16px_rgba(0,0,0,0.3))] sm:-top-44 sm:w-[30rem] md:-top-52 md:w-[36rem]"
      />

      <!-- Título con hojas. -->
      <img
        src={tituloCronicas.src}
        alt="Crónicas de la Tierra"
        width={tituloCronicas.width}
        height={tituloCronicas.height}
        class="mx-auto w-64 [filter:drop-shadow(0_4px_10px_rgba(0,0,0,0.25))] sm:w-80"
      />

      <!-- Tarjetas de posts. -->
      <div class="mt-8 grid gap-5 sm:mt-10 sm:grid-cols-2 sm:gap-6">
        {
          cronicas.map((p) => (
            <article class="flex flex-col overflow-hidden rounded-3xl bg-surface p-3 text-center shadow-lg">
              <Image
                src={p.img}
                alt={p.alt}
                widths={[320, 480, 640]}
                sizes="(min-width: 640px) 22rem, 90vw"
                format="webp"
                quality={72}
                loading="lazy"
                class="h-40 w-full rounded-2xl object-cover"
              />
              <h3 class="mt-4 px-2 text-base font-bold text-cta-strong">{p.title}</h3>
              <p class="mt-2 grow px-2 text-sm text-ink/70">{p.excerpt}</p>
              <a
                href={p.href}
                class="mx-2 mt-4 inline-flex items-center justify-center rounded-full bg-cta-strong px-6 py-2.5 font-display text-lg text-white transition hover:brightness-110 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-cta-strong"
              >
                Leer más
              </a>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- ¡A UN PEQUEÑO GALOPE DE LA CIUDAD! — ubicación + Villa Planes + cómo llegar. -->
  <section class="mx-auto max-w-6xl px-4 pt-16">
    <h2 class="section-title text-center">
      ¡A un pequeño galope de la ciudad!
    </h2>
    <p class="mx-auto mt-4 max-w-2xl text-center text-ink/80">
      Estamos en Tenjo, Cundinamarca. Tan cerca de Bogotá que no alcanzas a cansarte, pero lo
      suficientemente lejos para cambiar el ruido por el aire fresco.
    </p>

    <!-- Horarios + Ubicación (izq) junto al selfie con los animales (der). -->
    <div class="mt-10 grid items-center gap-8 md:grid-cols-[minmax(0,0.85fr)_minmax(0,1.15fr)]">
      <div class="grid gap-6 sm:grid-cols-2 md:grid-cols-1">
        <div class="rounded-3xl bg-cta-strong p-6 text-center text-white shadow-lg sm:p-7">
          <h3 class="font-display text-3xl text-[#FFF200] [text-shadow:0_1px_2px_rgba(0,0,0,0.35)] sm:text-4xl">
            Horarios
          </h3>
          <p class="mt-3 text-xl font-semibold">{visit.hoursLabel}</p>
          <p class="text-xl font-semibold">{visit.hours}</p>
        </div>
        <div class="rounded-3xl bg-cta-strong p-6 text-center text-white shadow-lg sm:p-7">
          <h3 class="font-display text-3xl text-[#FFF200] [text-shadow:0_1px_2px_rgba(0,0,0,0.35)] sm:text-4xl">
            Ubicación
          </h3>
          <p class="mt-3 text-lg font-semibold">{visit.address}</p>
          <p class="text-lg font-semibold text-white/90">{visit.city}</p>
          <a
            href={visit.maps}
            target="_blank"
            rel="noopener noreferrer"
            class="mt-3 inline-block text-base font-bold text-[#FFF200] underline-offset-4 hover:underline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
          >
            {visit.note} · Abrir en Maps →
          </a>
        </div>
      </div>

      <Image
        src={contactanos}
        alt="Don Juan, la mascota de Villa Juan, invitándote a contactarnos"
        widths={[320, 480, 620]}
        sizes="(min-width: 768px) 42vw, 70vw"
        format="webp"
        quality={80}
        loading="lazy"
        class="mx-auto w-full max-w-xs [filter:drop-shadow(0_8px_16px_rgba(0,0,0,0.25))]"
      />
    </div>

    <!-- Conoce los ¡Villa Planes! — selfie (izq) + botones de planes (der). -->
    <div class="mt-16">
      <img
        src={villaPlanesTitulo.src}
        alt="Conoce los ¡Villa Planes!"
        width={villaPlanesTitulo.width}
        height={villaPlanesTitulo.height}
        loading="lazy"
        class="mx-auto w-80 [filter:drop-shadow(0_4px_10px_rgba(0,0,0,0.25))] sm:w-[28rem]"
      />

      <div class="mt-8 grid items-end gap-8 md:grid-cols-[1.8fr_1fr]">
        <Image
          src={selfieAnimales}
          alt="Don Juan tomándose una selfie con todos los animales de la Ecogranja Villa Juan"
          widths={[480, 768, 1100, 1600]}
          sizes="(min-width: 768px) 64vw, 100vw"
          format="webp"
          quality={72}
          loading="lazy"
          class="w-full md:order-first"
        />

        <div>
          <p class="text-center text-[13px] font-bold text-white md:text-right">
            Descarga el <span class="font-extrabold">.pdf</span> de tu Villa Plan favorito*
          </p>
          <ul class="mt-5 space-y-4">
            {
              planesHome.map((p) => (
                <li>
                  <a
                    href={wa(`Quiero el Villa Plan ${p.title}`)}
                    target="_blank"
                    rel="noopener noreferrer"
                    aria-label={`Descargar el Villa Plan ${p.title}`}
                    style={`background-image:${p.grad};`}
                    class="flex items-center justify-center rounded-full px-6 py-5 text-center font-display text-3xl text-white shadow-lg ring-1 ring-black/10 transition hover:-translate-y-0.5 hover:brightness-105 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white sm:text-4xl [text-shadow:0_1px_3px_rgba(0,0,0,0.45)]"
                  >
                    {p.title}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>
  </section>

  <!-- ¿CÓMO LLEGAR DESDE BOGOTÁ? — franja naranja con 3 opciones de transporte.
       Sin margen superior: el selfie de arriba queda pegado a ras de esta franja. -->
  <section class="bg-cta py-16">
    <div class="mx-auto max-w-6xl px-4">
      <h2 class="text-center font-display text-4xl text-[#FFF200] [text-shadow:0_2px_4px_rgba(0,0,0,0.35)] sm:text-5xl">
        ¿Cómo llegar desde Bogotá?
      </h2>
      <div class="mt-10 grid gap-6 md:grid-cols-3">
        {
          llegar.map((t) => (
            <div class="flex flex-col">
              <div class="flex grow flex-col rounded-3xl bg-brand-strong p-6 text-white shadow-lg">
                <h3 class="text-center text-xl font-bold sm:text-2xl">{t.title}</h3>
                <p class="mt-4 grow text-center text-sm leading-relaxed text-white/90">{t.body}</p>
              </div>
              {t.pillHref ? (
                <a
                  href={t.pillHref}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="mt-5 flex w-full items-center justify-center rounded-full bg-[#FFF200] px-6 py-3.5 text-center font-display text-2xl text-cta-strong shadow-xl ring-4 ring-white/30 transition hover:-translate-y-0.5 hover:brightness-95 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
                >
                  {t.pill}
                </a>
              ) : (
                <span class="mt-5 flex w-full items-center justify-center rounded-full bg-[#FFF200] px-6 py-3.5 text-center font-display text-2xl text-cta-strong shadow-xl ring-4 ring-white/30">
                  {t.pill}
                </span>
              )}
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- GALERÍA — carrusel "coverflow": 3 fotos con marco naranja (centro al frente + laterales
       detrás, como el mockup) que rota automáticamente. Island de JS mínimo; respeta reduce-motion. -->
  <section class="mx-auto max-w-6xl px-4 py-16">
    <h2 class="section-title text-center">Galería</h2>
    <p class="mx-auto mt-4 max-w-2xl text-center text-ink/80">
      Momentos reales de familias, amigos y animales viviendo el campo en Ecogranja Villa Juan.
    </p>

    <div
      id="gallery"
      class="relative mx-auto mt-10 aspect-[3/2] w-full max-w-5xl overflow-hidden [perspective:1400px] sm:aspect-[5/2]"
      aria-roledescription="carrusel"
      aria-label="Galería Villa Juan"
    >
      {
        galeria.map((g, i) => (
          <figure
            class="gal-slide absolute top-1/2 left-1/2 w-[78%] cursor-pointer overflow-hidden rounded-[2rem] bg-cta p-2 shadow-2xl transition-all duration-700 ease-out will-change-transform motion-reduce:transition-none sm:w-[54%]"
            data-i={i}
            style={galInit[i] ?? galPos.center}
            aria-roledescription="slide"
            aria-label={`${i + 1} de ${galeria.length}`}
          >
            <Image
              src={g.img}
              alt={g.alt}
              widths={[480, 768, 1100]}
              sizes="(min-width: 640px) 34rem, 80vw"
              format="webp"
              quality={72}
              loading="lazy"
              class="aspect-[3/2] w-full rounded-[1.6rem] object-cover"
            />
          </figure>
        ))
      }

      <!-- Flechas (las activa el script) -->
      <button
        type="button"
        data-gal-prev
        aria-label="Foto anterior"
        class="absolute top-1/2 left-2 z-40 hidden -translate-y-1/2 rounded-full bg-black/40 p-2 text-white backdrop-blur-sm transition hover:bg-black/60 data-[on]:block sm:left-4"
      >
        <span aria-hidden="true" class="block h-6 w-6 text-2xl leading-none">‹</span>
      </button>
      <button
        type="button"
        data-gal-next
        aria-label="Foto siguiente"
        class="absolute top-1/2 right-2 z-40 hidden -translate-y-1/2 rounded-full bg-black/40 p-2 text-white backdrop-blur-sm transition hover:bg-black/60 data-[on]:block sm:right-4"
      >
        <span aria-hidden="true" class="block h-6 w-6 text-2xl leading-none">›</span>
      </button>
    </div>

    <!-- Dots (los activa el script) -->
    <div class="mt-6 flex justify-center gap-3">
      {
        galeria.map((_, i) => (
          <button
            type="button"
            data-gal-dot
            aria-label={`Ir a la foto ${i + 1}`}
            class="h-3 w-3 rounded-full bg-cta/40 ring-1 ring-black/10 transition hover:bg-cta aria-[current=true]:w-6 aria-[current=true]:bg-cta"
          />
        ))
      }
    </div>

    <div class="mt-8 text-center">
      <a
        href={social.instagram}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center justify-center rounded-full bg-brand-strong px-8 py-3 font-display text-xl text-white shadow-lg ring-1 ring-white/30 transition hover:-translate-y-0.5 hover:brightness-110 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white"
      >
        Síguenos en Instagram
      </a>
    </div>
  </section>

  <Faq />
</Base>

<script>
  // Enciende el barril ("on") gradualmente según el scroll. Respeta reduce-motion.
  const on = document.querySelector<HTMLElement>('.barril-on');
  if (on && !window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    const sec = (on.closest('section') as HTMLElement) ?? (on.parentElement as HTMLElement);
    on.style.opacity = '0';
    const update = () => {
      const r = sec.getBoundingClientRect();
      const vh = window.innerHeight;
      // La ignición ARRANCA cuando la sección se ve completa (bottom llega al viewport)
      // y termina cuando la sección llega arriba. Así el cambio ocurre a la vista.
      const fullTop = Math.max(1, vh - r.height); // top de la sección al quedar completa
      const p = Math.min(1, Math.max(0, (fullTop - r.top) / fullTop));
      on.style.opacity = String(p);
    };
    window.addEventListener('scroll', update, { passive: true });
    window.addEventListener('resize', update, { passive: true });
    update();
  }
</script>

<script>
  // Galería: carrusel "coverflow" con autoplay. Cada foto rota por 3 posiciones
  // (centro / derecha / izquierda). Flechas + dots + swipe + clic en un lateral.
  // Pausa en hover/focus. Respeta prefers-reduced-motion (invariante #4).
  const gallery = document.getElementById('gallery');
  if (gallery) {
    const slides = Array.from(gallery.querySelectorAll<HTMLElement>('.gal-slide'));
    const n = slides.length;
    const dots = Array.from(document.querySelectorAll<HTMLElement>('[data-gal-dot]'));
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    const POS: Record<string, { t: string; z: string; o: string; f: string }> = {
      center: { t: 'translate(-50%,-50%) scale(1)', z: '30', o: '1', f: 'none' },
      right: { t: 'translate(-6%,-50%) scale(0.82) rotateY(-18deg)', z: '20', o: '0.78', f: 'brightness(0.82)' },
      left: { t: 'translate(-94%,-50%) scale(0.82) rotateY(18deg)', z: '20', o: '0.78', f: 'brightness(0.82)' },
      hidden: { t: 'translate(-50%,-50%) scale(0.6)', z: '0', o: '0', f: 'none' },
    };
    const posFor = (rel: number) =>
      rel === 0 ? 'center' : rel === 1 ? 'right' : rel === n - 1 ? 'left' : 'hidden';

    let idx = 0;
    const render = () => {
      slides.forEach((el, i) => {
        const p = POS[posFor((i - idx + n) % n)];
        el.style.transform = p.t;
        el.style.zIndex = p.z;
        el.style.opacity = p.o;
        el.style.filter = p.f;
        el.setAttribute('aria-hidden', p.o === '0' ? 'true' : 'false');
      });
      dots.forEach((d, j) => d.setAttribute('aria-current', j === idx ? 'true' : 'false'));
    };
    const goTo = (i: number) => { idx = (i + n) % n; render(); };

    dots.forEach((d, i) => d.addEventListener('click', () => { goTo(i); restart(); }));
    slides.forEach((el, i) => el.addEventListener('click', () => { if (i !== idx) { goTo(i); restart(); } }));
    const prev = document.querySelector<HTMLElement>('[data-gal-prev]');
    const next = document.querySelector<HTMLElement>('[data-gal-next]');
    [prev, next].forEach((b) => b?.setAttribute('data-on', ''));
    prev?.addEventListener('click', () => { goTo(idx - 1); restart(); });
    next?.addEventListener('click', () => { goTo(idx + 1); restart(); });

    // Swipe táctil
    let x0: number | null = null;
    gallery.addEventListener('pointerdown', (e) => { x0 = e.clientX; });
    gallery.addEventListener('pointerup', (e) => {
      if (x0 === null) return;
      const dx = e.clientX - x0;
      if (Math.abs(dx) > 40) { goTo(idx + (dx < 0 ? 1 : -1)); restart(); }
      x0 = null;
    });

    // Autoplay (pausa en hover/focus; nada si reduce-motion)
    let timer: number | undefined;
    const stop = () => { if (timer) window.clearInterval(timer); };
    const start = () => { if (reduce) return; stop(); timer = window.setInterval(() => goTo(idx + 1), 3500); };
    const restart = () => { stop(); start(); };
    gallery.addEventListener('pointerenter', stop);
    gallery.addEventListener('pointerleave', start);
    gallery.addEventListener('focusin', stop);
    gallery.addEventListener('focusout', start);

    render();
    start();
  }
</script>

<script>
  // Villa Datos: entrada animada de los círculos + conteo de los números al entrar en vista.
  const dl = document.getElementById('villa-datos');
  if (dl) {
    const reduce = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const fmt = (n: number) => '+' + n.toString().replace(/\B(?=(\d{3})+(?!\d))/g, '.');
    const circles = Array.from(dl.querySelectorAll<HTMLElement>('.stat-circle'));
    const counters = Array.from(dl.querySelectorAll<HTMLElement>('[data-count]'));
    dl.setAttribute('data-anim', '');

    const runCount = (el: HTMLElement) => {
      const target = Number(el.dataset.count || '0');
      if (reduce) {
        el.textContent = fmt(target);
        return;
      }
      const dur = 1400;
      const t0 = performance.now();
      const step = (t: number) => {
        const p = Math.min(1, (t - t0) / dur);
        const eased = 1 - Math.pow(1 - p, 3);
        el.textContent = fmt(Math.round(target * eased));
        if (p < 1) requestAnimationFrame(step);
      };
      requestAnimationFrame(step);
    };

    const io = new IntersectionObserver(
      (entries, obs) => {
        entries.forEach((e) => {
          if (e.isIntersecting) {
            circles.forEach((c) => c.classList.add('is-in'));
            counters.forEach(runCount);
            obs.disconnect();
          }
        });
      },
      { threshold: 0.3 },
    );
    io.observe(dl);
  }
</script>
